# Write a program which takes a float and prints the absolute value of this float without using abs() function.

while True:

    num = float(input("please enter a number.\n"))

    import math

    print(abs(num))